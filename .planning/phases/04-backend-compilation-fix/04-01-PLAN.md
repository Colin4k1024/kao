---
phase: 04-backend-compilation-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/common/error.rs
  - backend/src/features/auth/service.rs
  - backend/src/features/auth/routes.rs
  - backend/src/features/config/routes.rs
  - backend/src/features/departments/repo.rs
  - backend/src/features/dictionary/data/routes.rs
  - backend/src/features/dictionary/type/routes.rs
  - backend/src/features/notice/routes.rs
  - backend/src/middleware/logger/mod.rs
autonomous: true
requirements: []
must_haves:
  truths:
    - "cargo check passes with 0 errors"
    - "cargo build succeeds"
    - "No AppError::Validation field initialization errors"
    - "No mismatched type errors in Router"
---

# Phase 04 Plan 01: Backend Compilation Fix

**Phase:** 04-backend-compilation-fix
**Plan:** 01
**Date:** 2026-03-28
**Status:** Planning

---

## Objective

Fix 57 Rust compilation errors blocking `cargo build`, `cargo test`, and Docker image.

**Error Breakdown:**
- 23 error[E0533]: expected value, found struct variant `AppError::Validation`
- 31 error[E0308]: mismatched types
- 2 error[E0716]: temporary value dropped while borrowed
- 1 error[E0277]: trait bound HeaderValue not satisfied

---

## Tasks

### Task 1: Fix AppError::Validation field initialization (23 errors)

<read_first>
backend/src/common/error.rs
backend/src/features/auth/service.rs
backend/src/features/auth/routes.rs
</read_first>

<action>
The error E0533 "expected value, found struct variant `AppError::Validation`" means `AppError::Validation` is being used as a value but it's a struct variant that requires field initialization.

**In `backend/src/common/error.rs`:**
Check how `AppError::Validation` is defined. If it's defined as a struct variant with fields, ensure all usages pass the required fields.

**In `backend/src/features/auth/service.rs` (lines 37, 56, etc.):**
Replace `AppError::Validation` with `AppError::Validation { field_name: "...".to_string(), message: "...".to_string() }` or the appropriate fields.

**In `backend/src/features/auth/routes.rs` (lines 45, 51):**
Same fix — provide required fields for `AppError::Validation` variant.

**In `backend/src/features/departments/repo.rs` (line 73):**
Same fix.

**In `backend/src/features/config/routes.rs` (lines 28, 39, 52, 64, 75):**
Same fix.

**In `backend/src/features/dictionary/data/routes.rs` (lines 30, 41, 52, 65, 77, 88):**
Same fix.

**In `backend/src/features/dictionary/type/routes.rs` (lines 29, 40, 53, 65, 76):**
Same fix.

**In `backend/src/features/notice/routes.rs` (lines 30, 41, 54, etc.):**
Same fix.

Read the `AppError` definition first to understand what fields each variant requires.
</action>

<acceptance_criteria>
- grep "AppError::Validation" backend/src/ returns all usages with proper field initialization
- cargo check 2>&1 | grep -c "E0533" returns 0
</acceptance_criteria>

<done>
All AppError::Validation variants properly initialized with required fields
</done>

### Task 2: Fix mismatched types (31 errors)

<read_first>
backend/src/features/config/routes.rs
backend/src/features/dictionary/data/routes.rs
backend/src/features/dictionary/type/routes.rs
backend/src/features/notice/routes.rs
</read_first>

<action>
The E0308 mismatched types errors are likely caused by:

1. Router type mismatch — ensure all route handlers return consistent types
2. Return type mismatch — handlers may return wrong type (e.g., String vs &str)
3. Axum router type inference issues

**Fix approach:**
1. Run `cargo check 2>&1 | grep "E0308" -A 5` to see specific mismatches
2. For each error, compare expected type vs actual type
3. Add type annotations where needed or convert types appropriately

Common fixes:
- `&str` vs `String`: Use `.as_str()` or convert with `.to_string()`
- `impl IntoResponse` vs specific type: Ensure handlers return consistent response types
- `Json<T>` vs `Result<Json<T>, AppError>`: Ensure error paths return proper error type

Check `backend/src/features/config/routes.rs` for specific line errors.
</action>

<acceptance_criteria>
- cargo check 2>&1 | grep -c "E0308" returns 0 or significantly reduced
- cargo check 2>&1 | grep -c "mismatched types" returns 0
</acceptance_criteria>

<done>
All mismatched type errors resolved
</done>

### Task 3: Fix temporary value lifetime (2 errors)

<read_first>
backend/src/middleware/logger/mod.rs
</read_first>

<action>
Fix the E0716 "temporary value dropped while borrowed" error in `backend/src/middleware/logger/mod.rs`.

The error at line 73 shows:
```
resp.headers_mut().insert(
    X_REQUEST_ID.as_str(),
    HeaderValue::from_str(request_id).unwrap_or_else(|_| HeaderValue::from_static("unknown")),
);
```

**Problem:** `HeaderValue::from_str(request_id)` creates a temporary that doesn't live long enough.

**Fix options:**
1. Store the HeaderValue in a variable first:
```rust
let header_value = HeaderValue::from_str(&request_id).unwrap_or_else(|_| HeaderValue::from_static("unknown"));
resp.headers_mut().insert(X_REQUEST_ID.as_str(), header_value);
```

2. Or use `.into()` if the type supports it

The same pattern applies to line 11 if it has the same issue.
</action>

<acceptance_criteria>
- cargo check 2>&1 | grep -c "E0716" returns 0
- No "temporary value dropped" errors in middleware/logger
</acceptance_criteria>

<done>
Temporary value lifetime errors fixed
</done>

### Task 4: Fix HeaderValue trait bound (1 error)

<read_first>
backend/src/middleware/logger/mod.rs
</read_first>

<action>
Fix the E0277 "trait bound HeaderValue: From<String> is not satisfied" error.

This likely means code is trying to do `HeaderValue::from(some_string)` where `some_string` is a `String`, but `HeaderValue` only implements `From<&str>`, not `From<String>`.

**Fix:** Convert String to &str:
```rust
HeaderValue::from_str(&string_var).unwrap_or_else(|_| HeaderValue::from_static("unknown"))
```

Or clone if needed:
```rust
HeaderValue::from_str(string_var.as_str())
```
</action>

<acceptance_criteria>
- cargo check 2>&1 | grep -c "E0277" returns 0
- HeaderValue construction uses proper From trait implementations
</acceptance_criteria>

<done>
HeaderValue trait bound errors resolved
</done>

### Task 5: Verify compilation

<action>
Run full compilation check:

```bash
cd backend
cargo check 2>&1
```

If errors remain, fix them based on the output. Common patterns:
- Additional type mismatches in router definitions
- Missing trait implementations
- Lifetime issues in complex closures

Final verification:
```bash
cd backend
cargo build --lib 2>&1 | tail -20
```
</action>

<acceptance_criteria>
- cargo check 2>&1 | grep -c "error\[" returns 0
- cargo build --lib succeeds
</acceptance_criteria>

<done>
cargo check passes, cargo build succeeds
</done>

---

## Success Criteria

| Criterion | Target |
|-----------|--------|
| cargo check | 0 errors |
| cargo build --lib | succeeds |
| E0533 AppError errors | 0 |
| E0308 mismatched types | 0 |
| E0716 temporary lifetime | 0 |
| E0277 trait bound | 0 |

---

**Generated:** 2026-03-28
**Phase:** 04-backend-compilation-fix
**Plan:** 01
**Wave:** 1
