---
phase: 05-docker-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Dockerfile
  - backend/entrypoint.sh
  - backend/migrate.sh
  - docker-compose.yml
  - playwright.config.ts
  - tests/e2e/
autonomous: true
requirements: []
must_haves:
  truths:
    - "Docker image builds successfully"
    - "docker-compose up works"
    - "Playwright E2E tests pass"
    - "Frontend and backend start correctly"
---

# Phase 05 Plan 01: Docker & E2E Testing

**Phase:** 05-docker-e2e
**Plan:** 01
**Date:** 2026-03-28
**Status:** Planning

---

## Objective

Build Docker image, verify docker-compose works, and run E2E tests to validate the full stack.

**Deliverables:**
1. Docker image builds successfully
2. docker-compose.yml works (backend + frontend + postgres)
3. Playwright E2E tests pass for login and core features

---

## Tasks

### Task 1: Verify Docker build

<read_first>
backend/Dockerfile
backend/entrypoint.sh
docker-compose.yml
</read_first>

<action>
Verify the Docker build works:

1. Check backend/Dockerfile exists and is correct
2. Check backend/entrypoint.sh is executable
3. Run:
```bash
cd backend
docker build -t kao-backend:latest .
```

If build fails, fix the Dockerfile based on the error output.

Common issues:
- Multi-stage build not configured correctly
- Missing dependencies
- Non-root user not set up
- Entrypoint script not executable
</action>

<acceptance_criteria>
- docker build -t kao-backend:latest . succeeds
- docker run --rm kao-backend:latest /health or equivalent returns success
</acceptance_criteria>

<done>
Docker image builds successfully
</done>

### Task 2: Verify docker-compose

<read_first>
docker-compose.yml
backend/Dockerfile
frontend/Dockerfile (if exists)
</read_first>

<action>
Verify docker-compose.yml works:

1. Check docker-compose.yml has all services (backend, frontend, postgres)
2. Check environment variables are properly configured
3. Run:
```bash
docker-compose up -d
docker-compose ps
docker-compose logs backend | tail -20
docker-compose logs frontend | tail -20
```

4. Verify services are healthy:
```bash
curl http://localhost:3000  # frontend
curl http://localhost:8080/health  # backend
```

If postgres is not available, start it first or use the existing database.
</action>

<acceptance_criteria>
- docker-compose up -d succeeds
- All services running (backend, frontend, postgres)
- Backend health check returns healthy
- Frontend is accessible
</acceptance_criteria>

<done>
docker-compose works with all services
</done>

### Task 3: Run Playwright E2E tests

<read_first>
playwright.config.ts
tests/e2e/
package.json
</read_first>

<action>
Run Playwright E2E tests:

1. Check if tests exist:
```bash
ls tests/e2e/
```

2. If tests don't exist, create basic E2E tests for:
   - Login page loads
   - User can login with valid credentials
   - Dashboard loads after login
   - Navigation works

3. Run tests:
```bash
cd /Users/ailabuser1/Desktop/gitcode/kao
npm test
# or
npx playwright test
```

4. If tests fail, fix the issues. Common problems:
   - Backend not running → start it first
   - Wrong port → check docker-compose port mappings
   - Auth token issues → check login flow

Required test files to create if missing:
- tests/e2e/login.spec.ts
- tests/e2e/dashboard.spec.ts
- tests/e2e/navigation.spec.ts
</action>

<acceptance_criteria>
- npx playwright test passes with 0 failures
- Login test works
- Dashboard test works
- Navigation test works
</acceptance_criteria>

<done>
E2E tests pass
</done>

### Task 4: Verify full stack integration

<action>
Verify the complete stack works together:

1. Start fresh environment:
```bash
docker-compose down -v
docker-compose up -d
```

2. Wait for services:
```bash
sleep 10
```

3. Run smoke tests:
```bash
# Backend API
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'

# Frontend
curl -I http://localhost:3000
```

4. Run full E2E suite:
```bash
npm test 2>&1 | tail -30
```

Expected: All services healthy, E2E tests pass.
</action>

<acceptance_criteria>
- Backend API responds correctly
- Frontend serves correctly
- E2E tests all pass
- No errors in docker-compose logs
</acceptance_criteria>

<done>
Full stack integration verified
</done>

---

## Success Criteria

| Criterion | Target |
|-----------|--------|
| docker build | succeeds |
| docker-compose up | all services healthy |
| Playwright tests | 0 failures |
| Backend API | responds correctly |
| Frontend | serves correctly |

---

**Generated:** 2026-03-28
**Phase:** 05-docker-e2e
**Plan:** 01
**Wave:** 1
