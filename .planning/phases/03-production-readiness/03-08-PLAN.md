---
phase: 03-production-readiness
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/services/ant-design-pro/api.ts
  - frontend/src/services/ant-design-pro/login.ts
  - frontend/src/services/swagger/store.ts
  - frontend/src/services/swagger/pet.ts
  - frontend/src/services/swagger/user.ts
  - frontend/src/components/RightContent/index.tsx
  - frontend/src/components/RightContent/AvatarDropdown.tsx
  - frontend/src/pages/table-list/components/CreateForm.tsx
  - frontend/src/pages/table-list/components/UpdateForm.tsx
  - frontend/src/pages/table-list/index.tsx
  - frontend/src/pages/user/login/index.tsx
  - frontend/src/features/auth/LoginForm.tsx
  - frontend/src/features/system/departments/DepartmentList.tsx
  - frontend/src/features/system/menus/MenuList.tsx
  - frontend/src/features/system/posts/PostList.tsx
  - frontend/src/features/system/roles/RoleList.tsx
  - frontend/src/features/system/users/UserList.tsx
  - frontend/src/components/common/PageLayout.tsx
  - frontend/src/components/dashboard/ChartSection.tsx
  - frontend/src/pages/job/index.tsx
  - frontend/src/pages/job/log/index.tsx
  - frontend/src/pages/monitoring/operation-log/index.tsx
  - frontend/src/pages/monitoring/login-log/index.tsx
  - frontend/src/pages/system/config/index.tsx
  - frontend/src/pages/system/notice/index.tsx
  - frontend/src/pages/system/dictionary/data/index.tsx
  - frontend/src/pages/system/dictionary/type/index.tsx
  - frontend/src/components/HeaderDropdown/index.tsx
  - frontend/src/pages/dashboard/index.tsx
  - frontend/src/pages/notice/index.tsx
  - frontend/src/pages/system/departments/DepartmentList.tsx
  - frontend/src/pages/system/menus/MenuList.tsx
  - frontend/src/pages/system/roles/RoleList.tsx
  - frontend/src/pages/system/users/UserList.tsx
  - frontend/src/requestErrorConfig.ts
autonomous: true
requirements:
  - NFR4
user_setup: []
must_haves:
  truths:
    - "npx tsc --noEmit passes with 0 errors"
    - "npm run build succeeds without TypeScript errors"
    - "@umijs/max imports replaced with Vite-compatible alternatives"
    - "API response type mismatches resolved"
    - "No implicit any types remain"
  artifacts:
    - path: "frontend/src/lib/umi-max-stub.ts"
      provides: "UmiJS compatibility stubs for Vite migration"
      contains: "useIntl, FormattedMessage, SelectLang, Helmet, useRequest, request"
    - path: "frontend/src/services/ant-design-pro/api.ts"
      provides: "API service with request function"
      contains: "export const request"
    - path: "frontend/src/features/auth/LoginForm.tsx"
      provides: "Login form component"
      contains: "ApiResponse<LoginResult>"
  key_links:
    - from: "frontend/src/lib/umi-max-stub.ts"
      to: "frontend/src/services/ant-design-pro/api.ts"
      via: "request function import"
      pattern: "from '@/lib/umi-max-stub'"
    - from: "frontend/src/lib/umi-max-stub.ts"
      to: "frontend/src/pages/user/login/index.tsx"
      via: "Helmet, SelectLang imports"
      pattern: "from '@/lib/umi-max-stub'"
---

# Phase 03 Plan 08: Frontend TypeScript Fix

**Phase:** 03-production-readiness
**Plan:** 08
**Date:** 2026-03-28
**Status:** Planning

---

## Objective

Fix 157 TypeScript errors blocking `npm run build` by replacing UmiJS imports with Vite-compatible alternatives.

**Purpose:** The frontend code was written for UmiJS framework but the project uses Vite + React Router. All `@umijs/max` imports must be replaced with the stub at `src/lib/umi-max-stub.ts`.

**Output:** All TypeScript errors resolved, `npm run build` succeeds.

---

## Execution Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

---

## Context

@../PROJECT.md
@../ROADMAP.md
@../STATE.md
@03-CONTEXT.md
@03-08-TYPESCRIPT-FIX.md

### Phase 3 Requirements

- **NFR4: Maintainability:** TypeScript strict mode, ESLint configuration

### Technical Background

The project migrated from UmiJS to Vite but 157 TypeScript errors remain from UmiJS-specific imports and types. The stub file `src/lib/umi-max-stub.ts` provides Vite-compatible replacements.

---

## Tasks

### Task 1: Replace @umijs/max imports in service files

<read_first>
frontend/src/lib/umi-max-stub.ts
frontend/src/services/ant-design-pro/api.ts
frontend/src/services/ant-design-pro/login.ts
frontend/src/services/swagger/store.ts
frontend/src/services/swagger/pet.ts
frontend/src/services/swagger/user.ts
</read_first>

<action>
In each of the following files, replace:
  import { request } from '@umijs/max';

With:
  import request from '@/lib/api';

The `@/lib/api.ts` already exports a `request` object with get/post/put/delete methods. Update these 5 files:

1. `frontend/src/services/ant-design-pro/api.ts` - Replace `from '@umijs/max'` with `from '@/lib/api'`
2. `frontend/src/services/ant-design-pro/login.ts` - Replace `from '@umijs/max'` with `from '@/lib/api'`
3. `frontend/src/services/swagger/store.ts` - Replace `from '@umijs/max'` with `from '@/lib/api'`
4. `frontend/src/services/swagger/pet.ts` - Replace `from '@umijs/max'` with `from '@/lib/api'`
5. `frontend/src/services/swagger/user.ts` - Replace `from '@umijs/max'` with `from '@/lib/api'`

If the import line does not exist (the request might be inline), verify the file uses the request from @/lib/api.ts via the interceptor chain in api.ts.
</action>

<acceptance_criteria>
- grep "from '@umijs/max'" frontend/src/services/ant-design-pro/api.ts returns empty
- grep "from '@umijs/max'" frontend/src/services/ant-design-pro/login.ts returns empty
- grep "from '@umijs/max'" frontend/src/services/swagger/store.ts returns empty
- grep "from '@umijs/max'" frontend/src/services/swagger/pet.ts returns empty
- grep "from '@umijs/max'" frontend/src/services/swagger/user.ts returns empty
- grep "from '@/lib/api'" frontend/src/services/ant-design-pro/api.ts returns match
</acceptance_criteria>

<done>
All 5 service files use request from @/lib/api instead of @umijs/max
</done>

### Task 2: Replace @umijs/max imports in page/component files

<read_first>
frontend/src/lib/umi-max-stub.ts
frontend/src/components/RightContent/index.tsx
frontend/src/components/RightContent/AvatarDropdown.tsx
frontend/src/pages/table-list/components/CreateForm.tsx
frontend/src/pages/table-list/components/UpdateForm.tsx
frontend/src/pages/table-list/index.tsx
frontend/src/pages/user/login/index.tsx
</read_first>

<action>
In each of the following files, replace UmiJS imports with stub imports:

1. `frontend/src/components/RightContent/index.tsx`:
   - Replace `import { SelectLang } from '@umijs/max';` with `import { SelectLang } from '@/lib/umi-max-stub';`

2. `frontend/src/components/RightContent/AvatarDropdown.tsx`:
   - If it imports SelectLang from @umijs/max, replace with `@/lib/umi-max-stub`

3. `frontend/src/pages/table-list/components/CreateForm.tsx`:
   - Replace `import { FormattedMessage, useIntl, useRequest } from '@umijs/max';` with `import { FormattedMessage, useIntl, useRequest } from '@/lib/umi-max-stub';`

4. `frontend/src/pages/table-list/components/UpdateForm.tsx`:
   - Replace `import { FormattedMessage, useIntl, useRequest } from '@umijs/max';` with `import { FormattedMessage, useIntl, useRequest } from '@/lib/umi-max-stub';`

5. `frontend/src/pages/table-list/index.tsx`:
   - Replace `import { FormattedMessage, useIntl, useRequest } from '@umijs/max';` with `import { FormattedMessage, useIntl, useRequest } from '@/lib/umi-max-stub';`

6. `frontend/src/pages/user/login/index.tsx`:
   - Replace `import { Helmet, SelectLang } from '@umijs/max';` with `import { Helmet, SelectLang } from '@/lib/umi-max-stub';`
</action>

<acceptance_criteria>
- grep "from '@umijs/max'" frontend/src/components/RightContent/index.tsx returns empty
- grep "from '@umijs/max'" frontend/src/components/RightContent/AvatarDropdown.tsx returns empty
- grep "from '@umijs/max'" frontend/src/pages/table-list/components/CreateForm.tsx returns empty
- grep "from '@umijs/max'" frontend/src/pages/table-list/components/UpdateForm.tsx returns empty
- grep "from '@umijs/max'" frontend/src/pages/table-list/index.tsx returns empty
- grep "from '@umijs/max'" frontend/src/pages/user/login/index.tsx returns empty
</acceptance_criteria>

<done>
All 6 component/page files use imports from @/lib/umi-max-stub instead of @umijs/max
</done>

### Task 3: Fix API response type mismatches

<read_first>
frontend/src/features/auth/LoginForm.tsx
frontend/src/features/system/departments/DepartmentList.tsx
frontend/src/features/system/menus/MenuList.tsx
frontend/src/features/system/posts/PostList.tsx
frontend/src/features/system/roles/RoleList.tsx
frontend/src/features/system/users/UserList.tsx
frontend/src/lib/api.ts
</read_first>

<action>
The API client at `frontend/src/lib/api.ts` already unwraps `.data` from responses (lines 73-86 return `res.data.data`). So code that accesses `.data` on API responses is double-accessing.

Fix each file by removing the extra `.data` accessor:

1. `frontend/src/features/auth/LoginForm.tsx`:
   - If code does `result.data` where result is from API call, remove one `.data` since api.ts already unwraps

2. `frontend/src/features/system/departments/DepartmentList.tsx`:
   - If accessing `.data` on Department[] responses, remove the extra `.data`

3. `frontend/src/features/system/menus/MenuList.tsx`:
   - If accessing `.data` on Menu[] responses, remove the extra `.data`

4. `frontend/src/features/system/posts/PostList.tsx`:
   - If accessing `.data` on PageResult<Post> responses, remove the extra `.data`

5. `frontend/src/features/system/roles/RoleList.tsx`:
   - If accessing `.data` on PageResult<Role> responses, remove the extra `.data`

6. `frontend/src/features/system/users/UserList.tsx`:
   - If accessing `.data` on PageResult<User> responses, remove the extra `.data`

The api.ts request methods return `Promise<T>` where T is already the unwrapped data type.
</action>

<acceptance_criteria>
- Each feature file uses response data directly without double .data access
- TypeScript compiles without "Property 'data' does not exist" errors for these files
</acceptance_criteria>

<done>
All 6 feature files correctly access API response data (single .data access or direct use)
</done>

### Task 4: Fix structural TypeScript issues

<read_first>
frontend/src/components/common/PageLayout.tsx
frontend/src/components/dashboard/ChartSection.tsx
frontend/src/pages/job/index.tsx
</read_first>

<action>
Fix the following structural issues:

1. `frontend/src/components/common/PageLayout.tsx` - CardInterface:
   - If using Card.Header, Card.Content, Card.Footer, ensure proper Card component usage
   - If CardInterface type issues exist, use standard divs or correct Card props

2. `frontend/src/components/dashboard/ChartSection.tsx` - ChartSectionProps type conflict:
   - If ChartSectionProps.type conflicts with CardProps.type, rename the `type` prop to `chartType` in ChartSectionProps
   - Update all usages of the `type` prop in the component to use `chartType`

3. `frontend/src/pages/job/index.tsx` - Duplicate Modal:
   - If there are duplicate Modal imports (e.g., from antd and @ant-design/icons), remove the duplicate
   - Keep only: `import { Modal } from 'antd';`

4. For any `import.meta.env` issues in api.ts:
   - The file already uses `import.meta.env.VITE_API_BASE_URL` which is correct for Vite
   - Ensure no other files use bare `import.meta.env` without VITE_ prefix
</action>

<acceptance_criteria>
- grep "Card.Header" frontend/src/components/common/PageLayout.tsx shows correct usage or is removed
- grep "type.*CardProps" frontend/src/components/dashboard/ChartSection.tsx returns empty (renamed to chartType)
- grep "duplicate.*Modal" frontend/src/pages/job/index.tsx returns empty
- No bare `import.meta.env` without VITE_ prefix in source files
</acceptance_criteria>

<done>
PageLayout Card usage fixed, ChartSectionProps.type renamed, duplicate Modal removed
</done>

### Task 5: Fix missing exports and type assertions

<read_first>
frontend/src/pages/job/log/index.tsx
frontend/src/pages/monitoring/operation-log/index.tsx
frontend/src/pages/monitoring/login-log/index.tsx
frontend/src/pages/system/config/index.tsx
frontend/src/pages/system/notice/index.tsx
frontend/src/pages/system/dictionary/data/index.tsx
frontend/src/pages/system/dictionary/type/index.tsx
</read_first>

<action>
Fix the following type issues:

1. `RefreshOutlined` does not exist in @ant-design/icons:
   - Replace `RefreshOutlined` with `ReloadOutlined` in all files that use it

2. `Text`, `TextArea` missing from antd:
   - If used as `antd.Text` or `antd.TextArea`, replace with standard HTML elements styled appropriately
   - Or use Typography component: `import { Typography } from 'antd';` and use `Typography.Text`

3. `ColumnsType` not found:
   - If using `ColumnsType` from antd, ensure proper import: `import type { ColumnsType } from 'antd/es/table/interface';`
   - Or use inline column definitions without explicit ColumnsType

4. Column `fixed` property type mismatch:
   - If `fixed: 'left'` or `fixed: 'right'` causes type error, cast to `FixedType`:
   - `fixed: 'left' as const` or `import type { FixedType } from 'antd/es/table/interface';`

5. `requestErrorConfig` function signature:
   - Ensure function matches expected signature or remove if unused

Check files:
- frontend/src/pages/job/log/index.tsx
- frontend/src/pages/monitoring/operation-log/index.tsx
- frontend/src/pages/monitoring/login-log/index.tsx
- frontend/src/pages/system/config/index.tsx
- frontend/src/pages/system/notice/index.tsx
- frontend/src/pages/system/dictionary/data/index.tsx
- frontend/src/pages/system/dictionary/type/index.tsx
</action>

<acceptance_criteria>
- grep "RefreshOutlined" frontend/src returns empty (replaced with ReloadOutlined)
- grep "antd\.Text" frontend/src returns empty (replaced with Typography.Text or styled div)
- grep "ColumnsType" frontend/src returns proper import or inline column definitions
- Column fixed props use proper typing or 'as const'
</acceptance_criteria>

<done>
All missing exports resolved, type assertions corrected
</done>

### Task 6: Remove unused imports and fix implicit any types

<read_first>
frontend/src/components/HeaderDropdown/index.tsx
frontend/src/pages/dashboard/index.tsx
frontend/src/pages/notice/index.tsx
frontend/src/requestErrorConfig.ts
</read_first>

<action>
Clean up TypeScript errors across the codebase:

1. Remove unused imports:
   - Search for `import.*from` statements where the imported item is not used
   - Common pattern: import { X } from 'antd' where X is not used

2. Fix implicit any types:
   - For function parameters without types: add explicit type annotations
   - For variables inferred from untyped sources: add explicit type annotations

Files to check (but scan all frontend/src for similar issues):
- frontend/src/components/HeaderDropdown/index.tsx
- frontend/src/pages/dashboard/index.tsx
- frontend/src/pages/notice/index.tsx
- frontend/src/requestErrorConfig.ts
- frontend/src/pages/system/departments/DepartmentList.tsx
- frontend/src/pages/system/menus/MenuList.tsx
- frontend/src/pages/system/roles/RoleList.tsx
- frontend/src/pages/system/users/UserList.tsx

For implicit any, common fixes:
- `function handleClick()` -> `function handleClick(e: MouseEvent)`
- `const [state, setState] = useState()` -> `const [state, setState] = useState<string>()`
- `const item =>` -> `const item: ItemType =>`
</action>

<acceptance_criteria>
- No unused imports causing TS errors
- No implicit any types causing TS errors
- npx tsc --noEmit shows significantly reduced error count
</acceptance_criteria>

<done>
Unused imports removed, implicit any types fixed
</done>

### Task 7: Verify TypeScript compilation

<action>
Run TypeScript compiler to verify all errors are fixed:

```bash
cd frontend
npx tsc --noEmit 2>&1 | head -100
```

If errors remain, fix them based on the output. Focus on:
- Any remaining @umijs/max imports
- Any remaining .data accessor issues
- Any remaining type mismatches

Final verification:
```bash
cd frontend
npm run build 2>&1 | tail -30
```

The build should complete without TypeScript errors.
</action>

<acceptance_criteria>
- npx tsc --noEmit 2>&1 | grep -c "error TS" returns 0 or close to 0
- npm run build completes without TypeScript errors
</acceptance_criteria>

<done>
TypeScript compilation passes, npm run build succeeds
</done>

---

## Verification

### TypeScript Check
```bash
cd frontend
npx tsc --noEmit 2>&1
```

Expected: 0 errors (or minimal errors that don't block build)

### Build Check
```bash
cd frontend
npm run build 2>&1
```

Expected: Build completes successfully

---

## Success Criteria

| Criterion | Target |
|-----------|--------|
| TypeScript errors | 0 |
| @umijs/max imports remaining | 0 |
| API response .data accessor issues | 0 |
| npm run build | Passes |
| npx tsc --noEmit | 0 errors |

---

**Generated:** 2026-03-28
**Phase:** 03-production-readiness
**Plan:** 08
**Wave:** 1
