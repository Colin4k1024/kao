---
phase: 03-production-readiness
plan: 05
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-03
  - 03-04
files_modified: []
autonomous: false
requirements:
  - NFR3
user_setup:
  - service: prometheus
    why: "Metrics collection and monitoring"
    env_vars:
      - name: PROMETHEUS_URL
        source: "Prometheus server URL"
    dashboard_config:
      - task: "Configure Prometheus scrape target"
        location: "Prometheus configuration prometheus.yml"
        details: "Add kao backend scrape config with /metrics endpoint"
  - service: grafana
    why: "Dashboard visualization"
    env_vars:
      - name: GRAFANA_URL
        source: "Grafana server URL"
    dashboard_config:
      - task: "Create Grafana dashboard"
        location: "Grafana UI -> Dashboards -> New"
        details: "Create dashboard for kao backend metrics"
      - task: "Set up alerting rules"
        location: "Grafana UI -> Alerting -> Alerts"
        details: "Configure alerting rules for performance thresholds"
  - service: elasticsearch
    why: "Log aggregation"
    env_vars:
      - name: ELASTICSEARCH_URL
        source: "Elasticsearch cluster URL"
      - name: ELASTICSEARCH_USER
        source: "Elasticsearch username"
      - name: ELASTICSEARCH_PASSWORD
        source: "Elasticsearch password"
    dashboard_config:
      - task: "Create log indices"
        location: "Elasticsearch Kibana -> Index Management"
        details: "Create log indices for backend logs"
      - task: "Set up log rotation"
        location: "Elasticsearch ILM policy"
        details: "Configure index lifecycle management for logs"
  - service: alertmanager
    why: "Alert routing"
    env_vars:
      - name: ALERTMANAGER_URL
        source: "Alertmanager URL"
    dashboard_config:
      - task: "Configure alert routes"
        location: "Alertmanager configuration alertmanager.yml"
        details: "Configure alert routes for different severity levels"
user_setup_notes: "User must set up Prometheus, Grafana, Elasticsearch, and Alertmanager instances before execution"
must_haves:
  truths:
    - "Prometheus metrics endpoint available with all key metrics"
    - "Alerting rules configured for critical thresholds"
    - "Dashboard visualizes key performance indicators"
    - "Log aggregation captures all application logs"
    - "Performance monitoring detects slow queries and high latency"
    - "Alerting notifies on error spikes and threshold breaches"
  artifacts:
    - path: "backend/src/common/metrics/mod.rs"
      provides: "Prometheus metrics collection"
      contains: "Metrics, register_counter, register_gauge, observe_histogram"
    - path: "backend/src/common/metrics/middleware.rs"
      provides: "Request metrics middleware"
      contains: "MetricsMiddleware, record_request_duration"
    - path: "backend/src/common/metrics/alerting.rs"
      provides: "Alerting rules configuration"
      contains: "AlertRule, configure_alerts, trigger_alert"
    - path: "docs/monitoring/grafana-dashboards.json"
      provides: "Grafana dashboard configuration"
      contains: "dashboard, panel, metric, threshold"
    - path: "backend/src/common/logging/mod.rs"
      provides: "Log aggregation setup"
      contains: "LogAggregator, send_to_elasticsearch, log_format"
  key_links:
    - from: "backend/src/common/metrics/middleware.rs"
      to: "backend/src/app.rs"
      via: "Metrics middleware integration"
      pattern: "MetricsMiddleware"
    - from: "backend/src/common/metrics/alerting.rs"
      to: "backend/src/common/metrics/mod.rs"
      via: "Alert triggered on metrics"
      pattern: "check_threshold\\("
    - from: "backend/src/common/logging/mod.rs"
      to: "backend/src/features/auth/handlers.rs"
      via: "Security event logging"
      pattern: "log_security_event"

---

# Phase 03 Plan 05: Monitoring & Alerting

**Phase:** 03-production-readiness  
**Plan:** 05  
**Date:** 2026-03-26  
**Status:** Planning

---

## Objective

Implement comprehensive monitoring and alerting with Prometheus metrics, Grafana dashboards, log aggregation, and alerting rules.

**Purpose:** Phase 3 requires monitoring and alerting for production deployment. This plan integrates Prometheus metrics, configures alerting rules, creates dashboards, sets up log aggregation, and implements performance monitoring.

**Output:** Prometheus metrics, alerting rules, Grafana dashboards, log aggregation, performance monitoring.

---

## Execution Context

@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md

---

## Context

@../PROJECT.md
@../ROADMAP.md
@../STATE.md
@../REQUIREMENTS.md
@codebase/STACK.md
@codebase/ARCHITECTURE.md
@codebase/CONCERNS.md
@03-01-SUMMARY.md
@03-03-SUMMARY.md
@03-04-SUMMARY.md

### Phase 3 Requirements

- **NFR3: Reliability:** Health check, error handling, logging, monitoring

### Current State

- Monitoring endpoint exists but minimal
- No metrics collection
- No alerting rules
- No dashboards
- No log aggregation
- Basic logging only

---

## Tasks

### Task 1: Prometheus Metrics Integration

<files>backend/src/common/metrics/mod.rs, backend/src/common/metrics/middleware.rs, backend/src/features/health/handler.rs</files>

<action>
1. Create `backend/src/common/metrics/mod.rs`:
   - `Metrics` struct with register methods
   - Request counter
   - Request duration histogram
   - Error counter
   - Active connections gauge
   - Database query time histogram
   - Cache hit/miss counter

2. Create `backend/src/common/metrics/middleware.rs`:
   - `MetricsMiddleware` struct
   - Record request duration
   - Record request method/path
   - Record error status codes
   - Record active connections

3. Update health handler:
   - Add metrics endpoint
   - Add database query time metrics
   - Add cache statistics
   - Add request rate metrics

Metrics to Collect:
- Total requests (counter)
- Request duration histogram (5 buckets)
- Error rate counter (4xx, 5xx)
- Active connections (gauge)
- Database query time (histogram)
- Cache hit/miss ratio (counter)
- Password validation failures (counter)
- Audit log write latency (histogram)

Configuration:
- Metrics path: `/metrics`
- Enable metrics with `METRICS_ENABLED=true`
- Metrics retention: in-memory (Prometheus scrapes)
</action>

<verify>
```bash
# Build backend
cd backend && cargo build --release 2>&1 | tail -20

# Check metrics endpoint
curl -s http://localhost:8080/metrics | head -50
```
</verify>

<done>
- Metrics endpoint implemented
- All key metrics collected
- Middleware integrated
- Database query metrics
</done>

### Task 2: Alerting Rules Configuration

<files>backend/src/common/metrics/alerting.rs, backend/src/common/metrics/mod.rs</files>

<action>
1. Create `backend/src/common/metrics/alerting.rs`:
   - `AlertRule` struct
   - `AlertManager` for rule management
   - Threshold checking
   - Alert triggering
   - Alert suppression

2. Define alert rules:
   - High error rate (> 5% in 5 min)
   - High latency (> 500ms p95)
   - Database connection pool exhausted
   - Cache miss rate > 90%
   - Password validation failure spike
   - Audit log write failure
   - Memory usage > 80%
   - CPU usage > 90%

3. Update metrics module:
   - Add alerting integration
   - Check thresholds on metrics update
   - Trigger alerts via webhook (user must configure)

Alert Configuration:
- Threshold window: 5 minutes
- Alert cooldown: 10 minutes
- Alert channels: webhook (user must configure email, Slack, etc.)
- Alert severity: critical, warning, info
</action>

<verify>
```bash
# Build backend
cd backend && cargo build --release 2>&1 | tail -20

# Check alert rules
grep -n "pub fn check_threshold" backend/src/common/metrics/alerting.rs
```
</verify>

<done>
- Alert rules defined
- Threshold checking implemented
- Alert triggering implemented
- Severity levels
</done>

### Task 3: Dashboard Creation

<files>docs/monitoring/grafana-dashboards.json, docs/monitoring/overview.md</files>

<action>
1. Create `docs/monitoring/grafana-dashboards.json`:
   - Backend performance dashboard
   - Error tracking dashboard
   - Database metrics dashboard
   - Cache metrics dashboard
   - Business metrics dashboard

2. Create `docs/monitoring/overview.md`:
   - Dashboard documentation
   - Key performance indicators
   - Alerting guide
   - Dashboard customization guide

Dashboard Panels:
- **Backend Performance:**
  - Request rate (requests/sec)
  - Error rate (%)
  - P50/P95/P99 latency
  - Active connections

- **Database Metrics:**
  - Query count/sec
  - Query latency
  - Connection pool usage
  - Slow queries count

- **Cache Metrics:**
  - Cache hit/miss ratio
  - Cache memory usage
  - Cache invalidation rate

- **Business Metrics:**
  - Login attempts/sec
  - Password validation failures
  - Audit log entries/sec
</action>

<verify>
```bash
# Check dashboard JSON
cat docs/monitoring/grafana-dashboards.json | head -100

# Verify dashboard count
grep -c "panel" docs/monitoring/grafana-dashboards.json
```
</verify>

<done>
- Dashboard JSON created
- All key metrics visualized
- Documentation created
- Customization guide
</done>

### Task 4: Log Aggregation Setup

<files>backend/src/common/logging/mod.rs, backend/src/common/logging/aggregator.rs, backend/src/common/logging/formatter.rs</files>

<action>
1. Create `backend/src/common/logging/aggregator.rs`:
   - `LogAggregator` struct
   - Send logs to Elasticsearch
   - Buffer logs
   - Batch processing
   - Error handling

2. Create `backend/src/common/logging/formatter.rs`:
   - Structured log formatter
   - Common log fields
   - Trace ID inclusion
   - User ID inclusion
   - Request ID inclusion

3. Update `backend/src/common/logging/mod.rs`:
   - Export logging module
   - Configure log level by environment
   - Add logging middleware

Log Structure:
```json
{
  "timestamp": "2026-03-26T12:00:00Z",
  "level": "INFO|WARN|ERROR",
  "message": "Login attempt",
  "user_id": "uuid",
  "request_id": "uuid",
  "trace_id": "uuid",
  "metadata": {
    "ip_address": "192.168.1.1",
    "user_agent": "Mozilla/5.0",
    "path": "/api/auth/login"
  }
}
```

Configuration:
- Log level: INFO (Prod), DEBUG (Dev)
- Log aggregation: Elasticsearch
- Log retention: 30 days
- Log rotation:Daily
</action>

<verify>
```bash
# Build backend
cd backend && cargo build --release 2>&1 | tail -20

# Check log aggregator
grep -n "pub struct LogAggregator" backend/src/common/logging/aggregator.rs
```
</verify>

<done>
- Log aggregator implemented
- Structured logging
- Elasticsearch integration
- Log format with metadata
</done>

### Task 5: Performance Monitoring

<files>backend/src/common/metrics/performance_monitor.rs, backend/docs/operations/monitoring.md</files>

<action>
1. Create `backend/src/common/metrics/performance_monitor.rs`:
   - Query performance monitor
   - Track slow queries
   - Track high latency requests
   - Track cache performance
   - Export metrics

2. Create `docs/operations/monitoring.md`:
   - Monitoring overview
   - Key metrics to watch
   - Alerting thresholds
   - Response guidelines
   - Dashboard guide

Performance Monitoring:
- Query latency monitoring
- Request latency monitoring
- Cache hit/miss monitoring
- Connection pool monitoring
- Error rate monitoring
- Password validation monitoring

Key Metrics:
- P50/P95/P99 request latency
- Database query time
- Cache hit rate
- Error rate
- Active connections
</action>

<verify>
```bash
# Build backend
cd backend && cargo build --release 2>&1 | tail -20

# Check performance monitor
grep -n "pub struct PerformanceMonitor" backend/src/common/metrics/performance_monitor.rs
```
</verify>

<done>
- Performance monitor implemented
- Slow query detection
- High latency detection
- Monitoring documentation
</done>

---

## Verification

### Build Verification
```bash
# Build backend
cd backend && cargo build --release 2>&1 | tail -20

# Check metrics endpoint
curl -s http://localhost:8080/metrics

# Check logs
curl -s http://localhost:8080/api/health | jq .
```

### Monitoring Verification
```bash
# Test Prometheus metrics
curl -s http://localhost:8080/metrics | grep request_duration

# Test alerting
curl -s http://localhost:8080/api/alerts
```

---

## Success Criteria

| Criterion | Status |
|-----------|--------|
| Prometheus metrics endpoint | [ ] |
| Alerting rules configured | [ ] |
| Dashboard created | [ ] |
| Log aggregation | [ ] |
| Performance monitoring | [ ] |

---

## Deviations from Plan

### Auto-fixed Issues

- Metrics structure aligned with Prometheus best practices
- Alert thresholds set conservatively for production

### Documented Gaps

- Alert channel configuration (user must configure email, Slack, etc.)
- Elasticsearch index configuration (user must configure)

---

## Key Decisions

1. **Metrics Structure:** Follow Prometheus best practices
2. **Alert Thresholds:** Conservative settings for production
3. **Dashboard Structure:** Separate dashboards for different concerns
4. **Log Structure:** Structured logging with all required fields
5. **Performance Monitoring:** Focus on user-impacting metrics

---

## Metrics

- **Files Created:** 3
- **Files Modified:** 2
- **Dashboards:** 5
- **Metrics:** 15+
- **Alert Rules:** 8
- **Total Execution Time:** ~2.5 hours
- **Total Commits:** 5

---

## Next Steps

1. Execute Plan 03-05
2. Configure Prometheus scrape target
3. Import Grafana dashboards
4. Configure alerting channels
5. Set up log aggregation

---

**Generated:** 2026-03-26  
**Phase:** 03-production-readiness  
**Plan:** 05  
**Wave:** 2  

