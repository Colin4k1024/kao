---
phase: 02-feature-completeness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/features/job/mod.rs
  - backend/src/features/job/routes.rs
  - backend/src/features/job/service.rs
  - backend/src/features/job/repo.rs
  - backend/src/features/job/log/mod.rs
  - backend/src/features/job/log/routes.rs
  - backend/src/features/job/log/service.rs
  - backend/src/features/job/log/repo.rs
  - backend/migrations/20240103000000_job.sql
  - backend/src/lib.rs
  - frontend/src/pages/job/index.tsx
  - frontend/src/pages/job/log/index.tsx
  - frontend/src/services/api/job.ts
autonomous: true
requirements:
  - FR7
must_haves:
  truths:
    - "Background job scheduler integrated with Rust backend"
    - "Job management API with complete CRUD operations"
    - "Cron expression validation implemented"
    - "Job execution logs tracked in database"
    - "Job status monitoring endpoint available"
  artifacts:
    - path: "backend/src/features/job/mod.rs"
      provides: "Job module exports"
      exports: ["JobController", "JobService", "JobRepository", "JobScheduler"]
    - path: "backend/src/features/job/log/mod.rs"
      provides: "Job log module exports"
      exports: ["JobLogController", "JobLogService", "JobLogRepository"]
    - path: "backend/migrations/20240103000000_job.sql"
      provides: "Job database schema"
      exports: ["sys_job", "sys_job_log"]
  key_links:
    - from: "backend/src/features/job/routes.rs"
      to: "backend/src/features/job/service.rs"
      via: "Route handlers call service"
      pattern: "job_router\\.(get|post|put|delete)"
    - from: "backend/src/features/job/service.rs"
      to: "backend/src/features/job/repo.rs"
      via: "Service calls repository"
      pattern: "job_repo\\.(find|create|update|delete|cron_validate)"
    - from: "backend/src/features/job/log/routes.rs"
      to: "backend/src/features/job/log/service.rs"
      via: "Job log route handlers call service"
      pattern: "job_log_router\\.(get|post|put|delete)"
---

# Phase 02 Plan 02: Scheduled Job Management

## Objective

Implement Scheduled Job Management including background job scheduler integration, job management API, job log tracking, cron expression validation, and job status monitoring.

## Purpose

Phase 2 core feature enables scheduled task execution for system automation and maintenance.

## Output

Complete Scheduled Job Management module with backend scheduler, API endpoints, and frontend monitoring.

## Execution Context

@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@backend/Cargo.toml

### Codebase References

**Phase 1 Completed:**
- Authentication foundation established
- JWT + bcrypt authentication
- Unified error response format
- Structured logging with tracing

**Phase 2 Requirements (FR7):**
- Background job scheduler integration
- Job management API
- Job log tracking
- Cron expression validation
- Job status monitoring

**Technology Evaluation:**
- Options: tokio-cron-scheduler, cron, kombucha, async-cron
- Candidate: tokio-cron-scheduler (best rust ecosystem integration)
- Alternative: cron crate (simple cron parsing)

---

## Tasks

<task type="auto">
  <name>Task 1: Evaluate and select job scheduler</name>
  <files>backend/Cargo.toml backend/src/features/job/mod.rs</files>
  <action>
    Evaluate job scheduling options and select appropriate crate:
    
    1. Evaluate tokio-cron-scheduler:
       - Add to Cargo.toml: tokio-cron-scheduler = "0.7"
       - Review API for Rust integration
       - Check cron expression support
       - Verify job persistence capabilities
    
    2. Evaluate cron crate:
       - Add to Cargo.toml: cron = "0.12"
       - Review API for Rust integration
       - Check cron expression support
       - Verify job persistence capabilities
    
    3. Evaluate async-cron:
       - Add to Cargo.toml: async-cron = "0.2"
       - Review API for Rust integration
       - Check cron expression support
    
    4. Decision criteria:
       - Cron expression validation support
       - Integration with existing Rust/Axum stack
       - Job persistence in PostgreSQL
       - Logging capabilities
       - Error handling
    
    5. Update Cargo.toml with selected crate
    6. Create backend/src/features/job/mod.rs with scheduler initialization
  </action>
  <read_first>
    backend/Cargo.toml
    backend/src/features/job/mod.rs
  </read_first>
  <verify>
    ```bash
    # Verify scheduler crate added to Cargo.toml
    grep "tokio-cron-scheduler" backend/Cargo.toml
    
    # Verify scheduler initialization
    grep "JobScheduler" backend/src/features/job/mod.rs
    
    # Run clippy on job module
    cd backend && cargo clippy --package kao --features test
    ```
  </verify>
  <done>
    - Job scheduler crate selected and added to Cargo.toml
    - Backend/src/features/job/mod.rs created
    - Scheduler initialization code written
    - Cron expression validation tested
  </done>
</task>

<task type="auto">
  <name>Task 2: Create job management module</name>
  <files>backend/src/features/job/mod.rs backend/src/features/job/routes.rs backend/src/features/job/service.rs backend/src/features/job/repo.rs backend/migrations/20240103000000_job.sql frontend/src/pages/job/index.tsx frontend/src/services/api/job.ts</files>
  <action>
    Create complete job management module:
    
    1. Update backend/src/features/job/mod.rs:
       - Export job controller, service, repository modules
       - Define Job struct with Cron field
       - Add JobStatus enum (PENDING, RUNNING, SUCCESS, FAILED)
       - Add JobScheduler type alias
    
    2. Create backend/src/features/job/routes.rs:
       - Define job routes (GET, POST, PUT, DELETE, SCHEDULE)
       - Apply authentication middleware
       - Apply authorization middleware (admin-only for create/delete)
       - Register routes in router
    
    3. Create backend/src/features/job/service.rs:
       - Implement create_job, update_job, delete_job
       - Implement schedule_job with cron validation
       - Implement validate_cron_expression function
       - Use selected cron crate for validation
       - Add error handling with unified error format
       - Add logging with tracing
    
    4. Create backend/src/features/job/repo.rs:
       - Implement SQL queries for sys_job table
       - Use parameterized queries
       - Add cron validation before storage
       - Handle job status updates
    
    5. Create migration backend/migrations/20240103000000_job.sql:
       - Create sys_job table
       - Add indexes on status and next_run_time
       - Add cron expression field
       - Add status field (PENDING, RUNNING, SUCCESS, FAILED)
       - Add sample jobs for testing
    
    6. Create frontend/src/pages/job/index.tsx:
       - Job list table with status badges
       - Create/Edit job form with cron expression input
       - Schedule/unschedule actions
       - Delete confirmation dialog
    
    7. Create frontend/src/services/api/job.ts:
       - Job API client functions
       - Request/response types
       - Cron expression validation on client
       - Error handling
  </action>
  <read_first>
    backend/src/features/job/mod.rs
    backend/src/features/job/service.rs
    backend/migrations/20240103000000_job.sql
    frontend/src/pages/job/index.tsx
    frontend/src/services/api/job.ts
  </read_first>
  <verify>
    ```bash
    # Test job scheduling with cron
    curl -s -X POST http://localhost:8080/api/system/jobs \
      -H "Authorization: Bearer <token>" \
      -H "Content-Type: application/json" \
      -d '{"name": "test-job", "cron_expression": "0 */5 * * * ?", "status": "PENDING"}' | jq .
    
    # Run tests
    cd backend && cargo test --package kao --features test
    ```
  </verify>
  <done>
    - Job scheduler crate added to Cargo.toml
    - Job CRUD endpoints created
    - Cron expression validation implemented
    - Job status tracking implemented
    - Frontend UI complete
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Create job log tracking</name>
  <files>backend/src/features/job/log/mod.rs backend/src/features/job/log/routes.rs backend/src/features/job/log/service.rs backend/src/features/job/log/repo.rs frontend/src/pages/job/log/index.tsx frontend/src/services/api/job.ts</files>
  <action>
    Create job log tracking module:
    
    1. Create backend/src/features/job/log/mod.rs:
       - Export job log controller, service, repository modules
       - Define JobLog struct with execution details
       - Add response types
    
    2. Create backend/src/features/job/log/routes.rs:
       - Define job log routes (GET, POST for execution)
       - Apply authentication middleware
    
    3. Create backend/src/features/job/log/service.rs:
       - Implement create_job_log, get_job_logs
       - Implement get_job_log_by_id
       - Add log filtering by job_id and status
       - Add execution count tracking
    
    4. Create backend/src/features/job/log/repo.rs:
       - Implement queries for sys_job_log table
       - Add indexes on job_id and run_time
       - Handle log persistence
    
    5. Update backend/migrations/20240103000000_job.sql:
       - Create sys_job_log table
       - Add foreign key to sys_job
       - Add indexes on job_id and run_time
       - Add sample logs for testing
    
    6. Create frontend/src/pages/job/log/index.tsx:
       - Job log list with execution status
       - Filter by job and execution status
       - Log details modal
       - Execution time statistics
    
    7. Update frontend/src/services/api/job.ts:
       - Add job log API client functions
       - Request/response types
       - Error handling
  </action>
  <read_first>
    backend/src/features/job/log/mod.rs
    backend/migrations/20240103000000_job.sql
    frontend/src/pages/job/log/index.tsx
    frontend/src/services/api/job.ts
  </read_first>
  <verify>
    ```bash
    # Test job log list
    curl -s "http://localhost:8080/api/system/jobs/logs?job_id=1" | jq .
    
    # Run tests
    cd backend && cargo test --package kao --features test
    ```
  </verify>
  <done>
    - Job log tracking endpoints created
    - Log filtering by job and status working
    - Frontend UI with execution details
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement job status monitoring</name>
  <files>backend/src/features/job/mod.rs backend/src/features/job/service.rs backend/src/app.rs backend/middleware/auth/mod.rs</files>
  <action>
    Implement job status monitoring endpoint:
    
    1. Update backend/src/features/job/mod.rs:
       - Add monitor module for status statistics
       - Export status statistics functions
    
    2. Update backend/src/features/job/service.rs:
       - Implement get_job_stats function
       - Calculate total jobs, running, completed, failed
       - Add last_run_time tracking
       - Add status distribution statistics
    
    3. Update backend/src/app.rs:
       - Add /api/system/jobs/status endpoint
       - Return structured status response
       - Include job statistics
    
    4. Create response struct for job status:
       ```rust
       pub struct JobStatusResponse {
         pub total: i64,
         pub pending: i64,
         pub running: i64,
         pub completed: i64,
         pub failed: i64,
         pub last_run_at: Option<DateTime<Utc>>,
         pub next_run_at: Option<DateTime<Utc>>,
       }
       ```
    
    5. Test endpoint returns correct statistics
  </action>
  <read_first>
    backend/src/features/job/mod.rs
    backend/src/features/job/service.rs
    backend/src/app.rs
  </read_first>
  <verify>
    ```bash
    # Test job status endpoint
    curl -s http://localhost:8080/api/system/jobs/status | jq .
    
    # Run tests
    cd backend && cargo test --package kao --features test
    ```
  </verify>
  <done>
    - Job status endpoint created
    - Job statistics calculated correctly
    - Frontend can display job status
    - All tests pass
  </done>
</task>

## Verification

```bash
# Test job scheduling with cron
curl -s -X POST http://localhost:8080/api/system/jobs \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"name": "test-job", "cron_expression": "0 */5 * * * ?", "status": "PENDING"}' | jq .

# Test job list with stats
curl -s http://localhost:8080/api/system/jobs/status | jq .

# Test job log list
curl -s "http://localhost:8080/api/system/jobs/logs?job_id=1" | jq .

# Run tests
cd backend && cargo test --package kao --features test
```

## Success Criteria

- [ ] Background job scheduler integrated
- [ ] Job CRUD endpoints working
- [ ] Cron expression validation implemented
- [ ] Job log tracking working
- [ ] Job status monitoring endpoint available
- [ ] Frontend UI complete
- [ ] Test coverage >50%
- [ ] Security vulnerabilities fixed
