---
phase: 01-stabilization-security
plan: 03
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - backend/src/repositories/user_repo.rs
  - backend/src/common/db.rs
  - backend/src/config/settings.rs
  - backend/migrations/20240101000000_init.sql
  - backend/Dockerfile
  - backend/src/common/auth/claims.rs
autonomous: true
requirements:
  - FR7
  - NFR2
  - NFR3
must_haves:
  truths:
    - "Database queries use correct table names (sys_users, sys_roles, etc.)"
    - "Database migrations run automatically on startup"
    - "Connection pool is properly configured with environment variables"
    - "JWT Claims struct matches across all implementations"
  artifacts:
    - path: "backend/src/repositories/user_repo.rs"
      provides: "User repository with correct table names"
      exports: ["UserRepository", "find_by_username", "find_by_id"]
    - path: "backend/src/common/db.rs"
      provides: "Database connection pool configuration"
      exports: ["get_pool", "PgPool"]
    - path: "backend/src/common/auth/claims.rs"
      provides: "JWT Claims struct"
      exports: ["Claims", "TokenData"]
  key_links:
    - from: "backend/src/repositories/user_repo.rs"
      to: "backend/migrations/20240101000000_init.sql"
      via: "Table name validation"
      pattern: "sys_users|sys_roles|sys_departments"
    - from: "backend/src/common/db.rs"
      to: "backend/src/config/settings.rs"
      via: "Pool configuration"
      pattern: "DatabaseSettings|max_connections"
    - from: "backend/src/common/auth/claims.rs"
      to: "backend/src/utils/jwt.rs"
      via: "Claims consistency"
      pattern: "Claims.*struct|token.*claims"
---

# Phase 01 Plan 03: Database & Migrations

## Objective

Audit database queries against schema, ensure migrations run automatically on startup, configure connection pool, and fix JWT Claims struct mismatch.

## Purpose

Database issues cause query failures and security vulnerabilities. Phase 1 requires correct table names, proper migrations, and consistent JWT structure.

## Output

Fixed database migrations, automatic migration execution, connection pool configuration, and consistent JWT claims.

## Execution Context

@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONCERNS.md
@backend/src/common/db.rs
@backend/src/repositories/user_repo.rs

### Codebase References

**Current Issues (from CONCERNS.md):**

1. **Table name mismatch (user_repo.rs:27):** Queries use `users` but migration creates `sys_users`
2. **No migration execution:** Dockerfile copies migrations but doesn't run them
3. **No connection pool config:** Pool created without limits
4. **JWT Claims mismatch:** Different structs in claims.rs vs jwt.rs

---

## Tasks

<task type="auto">
  <name>Task 1: Fix database table name mismatches</name>
  <files>backend/src/repositories/user_repo.rs backend/src/repositories/role_repo.rs backend/src/repositories/department_repo.rs backend/src/models/*.rs</files>
  <action>
    Audit all table names in repository files:
    1. Find all references to `users` table
    2. Replace with `sys_users`
    3. Find all references to `roles` table
    4. Replace with `sys_roles`
    5. Find all references to `departments` table
    6. Replace with `sys_departments`
    7. Find all references to `menus` table
    8. Replace with `sys_menu`
    
    Update model files:
    - Update table_name attribute if present
    - Update SQL queries in repo files
    
    Files to check:
    - backend/src/repositories/user_repo.rs
    - backend/src/repositories/role_repo.rs
    - backend/src/repositories/department_repo.rs
    - backend/src/models/user.rs
    - backend/src/models/role.rs
    - backend/src/models/department.rs
  </action>
  <read_first>
    backend/src/repositories/user_repo.rs
    backend/src/repositories/role_repo.rs
    backend/src/repositories/department_repo.rs
  </read_first>
  <acceptance_criteria>
    - All queries use sys_users (not users)
    - All queries use sys_roles (not roles)
    - All queries use sys_departments (not departments)
    - All queries use sys_menu (not menus)
    - No hardcoded table names remain
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Configure database connection pool</name>
  <files>backend/src/common/db.rs backend/src/config/settings.rs backend/src/main.rs</files>
  <action>
    Update db.rs for proper connection pooling:
    1. Create DatabaseSettings struct with:
       - url: String
       - max_connections: u32 (default 20)
       - min_connections: u32 (default 5)
       - idle_timeout: u32 (default 300 seconds)
       - connect_timeout: u32 (default 10 seconds)
    
    2. Update get_pool function:
       - Read settings from env
       - Create pool with settings
       - Validate connection before returning
    
    3. Update main.rs:
       - Load DatabaseSettings
       - Create pool with settings
       - Pass pool to app
    
    Update Cargo.toml:
    - Add async-postgres-pool = "0.5" (or use sqlx::Pool directly)
  </action>
  <read_first>
    backend/src/common/db.rs
    backend/src/config/settings.rs
    backend/src/main.rs
  </read_first>
  <acceptance_criteria>
    - DatabaseSettings has all pool config fields
    - max_connections = 20
    - min_connections = 5
    - idle_timeout = 300s
    - connect_timeout = 10s
    - get_pool validates connection
    -main.rs creates pool with settings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement automatic migration execution</name>
  <files>backend/src/common/db.rs backend/Dockerfile backend/src/main.rs</files>
  <action>
    Add automatic migration execution:
    1. Create run_migrations function in db.rs:
       - Connect to database
       - Run sqlx migrate apply
       - Return result with migration status
    
    2. Update main.rs:
       - Call run_migrations before starting server
       - Handle migration errors gracefully
       - Log migration status
    
    3. Update Dockerfile:
       - Ensure migrations directory copied
       - Add healthcheck that waits for DB
       - Add migration timeout
    
    Migration directory structure:
    - backend/migrations/
    - 20240101000000_init.sql (existing)
    - Add new migrations as needed
  </action>
  <read_first>
    backend/src/common/db.rs
    backend/Dockerfile
    backend/src/main.rs
  </read_first>
  <acceptance_criteria>
    - run_migrations function exists in db.rs
    - main.rs calls run_migrations on startup
    - Dockerfile has healthcheck
    - Migrations directory exists
    - Migration errors handled gracefully
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Fix JWT Claims struct mismatch</name>
  <files>backend/src/common/auth/claims.rs backend/src/utils/jwt.rs backend/src/common/auth/jwt.rs</files>
  <action>
    Consolidate JWT Claims implementation:
    1. Read claims.rs and jwt.rs to understand mismatch
    2. Decide on single source of truth for Claims struct
    3. Update all implementations to use single Claims
    4. Add unit tests for Claims serialization
    
    Claims struct should include:
    - user_id: Uuid
    - username: String
    - email: String
    - roles: Vec<String>
    - exp: usize (expiration timestamp)
    - iat: usize (issued at timestamp)
    
    Update:
    - claims.rs: single source of truth
    - jwt.rs: use Claims from claims.rs
    - auth_service.rs: use Claims from claims.rs
    - extractors: use Claims from claims.rs
  </action>
  <read_first>
    backend/src/common/auth/claims.rs
    backend/src/utils/jwt.rs
    backend/src/common/auth/jwt.rs
  </read_first>
  <acceptance_criteria>
    - Single Claims struct sources
    - All files import from claims.rs
    - Claims has all required fields
    - Unit tests for Claims serialization
    - No duplicate Claims definitions
  </acceptance_criteria>
</task>

## Verification

```bash
# Check table names
grep -rn "users\|roles\|departments\|menus" backend/src/repositories/ --include="*.rs" | grep -v "sys_" || echo "All table names use sys_ prefix"

# Check connection pool config
grep -n "max_connections\|min_connections" backend/src/common/db.rs

# Check migration execution
grep -n "migrate" backend/src/main.rs

# Check Claims consistency
grep -n "Claims.*struct" backend/src/common/auth/claims.rs
grep -n "use.*Claims" backend/src/common/auth/jwt.rs
```

## Success Criteria

- [ ] All queries use correct table names (sys_users, sys_roles, etc.)
- [ ] Connection pool properly configured
- [ ] Migrations run automatically on startup
- [ ] JWT Claims struct consistent across all files
- [ ] No table name mismatches
- [ ] Unit tests for Claims

