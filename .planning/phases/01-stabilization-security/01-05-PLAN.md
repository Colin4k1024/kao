---
phase: 01-stabilization-security
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified:
  - backend/src/app.rs
  - backend/src/common/response.rs backend/src/common/error.rs
  - backend/src/middleware/logger.rs backend/src/middleware/mod.rs
  - backend/src/common/db.rs
  - frontend/src/services/api/core.ts frontend/src/components/LoadingError.tsx
autonomous: true
requirements:
  - NFR2
  - NFR3
must_haves:
  truths:
    - "Health check endpoint returns actual status checks"
    - "Structured logging includes authentication events"
    - "Request tracking headers added to all responses"
    - "Consistent error handling across all endpoints"
  artifacts:
    - path: "backend/src/app.rs"
      provides: "Health check endpoint with dependency checks"
      exports: ["/health"]
    - path: "backend/src/middleware/logger.rs"
      provides: "Structured logging middleware"
      exports: ["RequestLogger", "LoggerLayer"]
    - path: "backend/src/common/response.rs"
      provides: "Unified response format"
      exports: ["ApiResponse", "ResponseData"]
  key_links:
    - from: "backend/src/app.rs"
      to: "backend/src/common/db.rs"
      via: "Health check database check"
      pattern: "/health|database_status"
    - from: "backend/src/middleware/logger.rs"
      to: "backend/src/app.rs"
      via: "Request logging"
      pattern: "log!|trace!|info!|error!"
    - from: "backend/src/common/response.rs"
      to: "backend/src/common/error.rs"
      via: "Error handling"
      pattern: "AppError|into_response"
---

# Phase 01 Plan 05: Observability

## Objective

Implement proper health check endpoint with dependency status, structured logging for authentication events, and request tracking headers.

## Purpose

Phase 1 observability requires health checks with actual status checks (not just static OK), structured logging, and request tracking for debugging.

## Output

Production-ready observability: health checks, structured logging, and request tracking.

## Execution Context

@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONCERNS.md
@backend/src/app.rs

### Codebase References

**Current Issues (from CONCERNS.md):**

1. **Health check (app.rs line 20):** Only returns static "OK" with no dependency checks
2. **No structured logging:** Existing logging is basic println!/println!
3. **No request tracking:** No way to trace requests across services

---

## Tasks

<task type="auto">
  <name>Task 1: Implement proper health check endpoint</name>
  <files>backend/src/app.rs backend/src/common/db.rs backend/src/common/response.rs backend/src/common/error.rs</files>
  <action>
    Update health check endpoint:
    1. Create health endpoint at /health
    2. Check.database connection:
       - Connect to database
       - Run simple query (SELECT 1)
       - Return error if connection fails
    3. Check Redis (if configured):
       - Ping Redis server
       - Return warning if unavailable
    4. Return structured response:
       ```json
       {
         "status": "healthy|degraded|unhealthy",
         "checks": {
           "database": "ok|error|warning",
           "redis": "ok|error|warning (optional)"
         },
         "timestamp": "2024-01-01T00:00:00Z"
       }
       ```
    
    Update error handling:
    - Database error → status: unhealthy
    - Redis unavailable → status: degraded
    - All healthy → status: healthy
  </action>
  <read_first>
    backend/src/app.rs
    backend/src/common/db.rs
  </read_first>
  <acceptance_criteria>
    - /health endpoint returns structured response
    - database check implemented
    - Redis check (optional, with warning if unavailable)
    - status field with healthy/degraded/unhealthy
    - checks object with per-dependency status
    - timestamp included
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add structured logging for authentication events</name>
  <files>backend/src/middleware/logger.rs backend/src/main.rs backend/src/api/auth/handlers.rs backend/src/services/auth_service.rs</files>
  < action>
    Create structured logging middleware:
    1. Update logger.rs with structured logging:
       - Access log with request ID, timestamp, method, path, status
       - Error log with error type, message, backtrace
       - Auth log with user_id, action, success/failure
    
    2. Update main.rs:
       - Configure tracing subscriber
       - Add structured logging layer
       - Set log level from environment
    
    3. Update handlers.rs and auth_service.rs:
       - Use tracing::info! for auth successes
       - Use tracing::warn! for auth failures
       - Use tracing::error! for authentication errors
       - Include user_id, ip_address, user_agent
    
    Examples:
    ```rust
    tracing::info!(username = %username, ip = %ip, "Login successful");
    tracing::warn!(username = %username, ip = %ip, attempts = attempts, "Login failed");
    tracing::error!(username = %username, error = %error, "Authentication error");
    ```
  </action>
  <read_first>
    backend/src/middleware/logger.rs
    backend/src/api/auth/handlers.rs
    backend/src/services/auth_service.rs
  </read_first>
  <acceptance_criteria>
    - logger.rs implements structured logging
    - logging layer added to main.rs
    - Login success logged with username
    - Login failure logged with attempt count
    - Authentication errors logged with full details
    - Log level configurable via environment
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add request tracking headers</name>
  <files>backend/src/middleware/logger.rs backend/src/common/response.rs backend/src/app.rs</files>
  <action>
    Add request tracking:
    1. Update logger middleware to:
       - Generate unique request ID for each request
       - Add request ID to response header (X-Request-ID)
       - Log request ID with all request logs
    
    2. Update response.rs:
       - Add request_id field to ApiResponse
       - Include request_id in all responses
    
    3. Update app.rs:
       - Extract request ID from header or generate new
       - Pass request ID through middleware
       - Log request ID on response
    
    Response header:
    ```http
    X-Request-ID: abc123-def456-xyz789
    ```
    
    Response body:
    ```json
    {
      "code": 200,
      "message": "success",
      "data": {...},
      "request_id": "abc123-def456-xyz789"
    }
    ```
  </action>
  <read_first>
    backend/src/middleware/logger.rs
    backend/src/common/response.rs
  </read_first>
  <acceptance_criteria>
    - Request ID generated for each request
    - X-Request-ID header added to responses
    - Request ID logged with all requests
    - Request ID included in response body
    - Request ID consistent across middleware
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Setup error handling structure</name>
  <files>backend/src/common/error.rs backend/src/common/response.rs backend/src/api/auth/handlers.rs backend/src/middleware/cors.rs</files>
  <action>
    Consolidate error handling:
    1. Update error.rs with AppError enum:
       - DatabaseError { source: sqlx::Error }
       - AuthError { message: String }
       - ValidationError { field: String, message: String }
       - NotFoundError { resource: String }
       - RateLimitError { retry_after: u64 }
       - InternalError { source: Option<String> }
    
    2. Update response.rs:
       - ApiResponse with unified format
       - IntoResponse trait for Axum compatibility
       - Error responses follow format
    
    3. Update handlers and middleware:
       - Use AppError for all errors
       - Return unified error format
       - Preserve error details for debugging
    
    Error response format:
    ```json
    {
      "code": 400,
      "message": "Validation error",
      "data": {
        "error_type": "ValidationError",
        "field": "email",
        "details": "Invalid email format"
      }
    }
    ```
  </action>
  <read_first>
    backend/src/common/error.rs
    backend/src/common/response.rs
  </read_first>
  <acceptance_criteria>
    - AppError enum with all error types
    - Unified error response format
    - Database errors wrapped in AppError
    - Auth errors wrapped in AppError
    - Validation errors include field name
    - All handlers use AppError
  </acceptance_criteria>
</task>

## Verification

```bash
# Test health endpoint
curl -s http://localhost:8080/health | jq .

# Run with logging enabled
RUST_LOG=info cargo run

# Check request ID in response
curl -s -I http://localhost:8080/api/system/users | grep X-Request-ID

# Test error response
curl -s http://localhost:8080/api/auth/login -X POST -d '{}' | jq .
```

## Success Criteria

- [ ] Health check returns actual dependency status
- [ ] Structured logging includes auth events
- [ ] Request tracking headers present
- [ ] Unified error handling across all endpoints
- [ ] All error responses follow format
- [ ] Log level configurable

